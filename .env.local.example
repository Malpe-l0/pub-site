# Copia questo file in .env.local e compila i valori.

# Password per entrare nel pannello /admin
ADMIN_PASSWORD=""

# Segreto per firmare il cookie di sessione: stringa lunga e casuale.
# Per generarne uno: openssl rand -base64 32
SESSION_SECRET=""

# Facoltativo: cartella dove vivono database e foto (default: ./data)
# DATA_DIR="/var/lib/pub-site"
